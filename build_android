#!/usr/bin/env bash
set -euo pipefail

npm ci
npm run build
if [ ! -d android ]; then
  npx cap add android
fi
npx cap sync android
cd android
./gradlew assembleDebug
cd ..
mkdir -p APK
cp android/app/build/outputs/apk/debug/app-debug.apk APK/app-debug.apk
echo "APK written to APK/app-debug.apk"
